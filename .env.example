# Weight Tracker Environment Variables

# Required: Secret for accessing the application
# This should be a secure, random string
APP_SECRET=your_secret_here

# Optional: Override default database path
# DATABASE_PATH=/data/tracker.db

# Optional: Override default port (internal port, don't change unless needed)
# PORT=3001

# Docker Compose specific variables
# Set the external port for the application
EXTERNAL_PORT=3000

# Production external port (for docker-compose.prod.yml)
PROD_EXTERNAL_PORT=80